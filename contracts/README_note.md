# contracts — B5 CI smoke test

Temporary file; will be removed in the next PR. Purpose: trigger the four
required status checks (legacy-name-guard, betterleaks, pr-title-lint,
signature-verify) via a no-op PR to validate the hull reusable-workflow
inheritance model end-to-end (plan step B5).
