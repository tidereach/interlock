# CHANGELOG — integrity-inputs

## v1.0.0 (2026-06-30)

Initial schema. Pinned three sieve-owned entries: `pattern_table`, `model_digest`, `prompt_template` (serialization specs per `migration/MAIN.md § 10` in tidereach/hull). Four TBD entries named with graduation conditions: `sandbox_config_hash` (airlock Stage 4), `hook_manifest_digest` (interlock Stage 2), `ruleset_hash` (interlock Stage 2 policy module), `engine_ipc_version` (arbiter Stage 5).
